mcp[cli]>=1.10.0,<2.0.0
monarchmoneycommunity>=1.5.2
gql>=3.4
keyring>=24.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0
# Floors on transitive dependencies, held only to keep known-vulnerable
# versions out of a fresh resolve. Neither is imported directly.
#   idna    >=3.15   GHSA-65pc-fj4g-8rjx (medium) -- idna.encode() bypass
#   aiohttp >=3.14.3 GHSA-cq5v-8q36-5273 (high)
idna>=3.15
aiohttp>=3.14.3
